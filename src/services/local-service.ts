import {IAuthenticate} from '~model/authenticate';
import {IProfile} from '~model/profile';

class LocalService {
  token: IAuthenticate = {
    accessToken: '',
    refreshToken: '',
  };

  //   profile: IProfile = {};
}
